import styled from "styled-components";

const DisplayContainer = styled.div`
    height: 100vh;
    margin-top: 50px;
`

const ButtonReset = styled.button`
    position: absolute;
    bottom: 0;
`

export {DisplayContainer, ButtonReset}